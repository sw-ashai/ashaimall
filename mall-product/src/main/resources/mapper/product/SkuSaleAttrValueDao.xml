<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="icu.ashai.mall.product.dao.SkuSaleAttrValueDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="icu.ashai.mall.product.entity.SkuSaleAttrValueEntity" id="skuSaleAttrValueMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>
    <resultMap id="skuItemSaleAttrVo" type="icu.ashai.mall.product.vo.SkuItemVo$SkuItemSaleAttrVo">
        <result property="attrName" column="attr_name"/>
        <result property="attrId" column="attr_id"/>
        <collection property="attrValues"
                    ofType="icu.ashai.mall.product.vo.SkuItemVo$AttrValueWithSkuIdVo">
            <result property="attrValue" column="attr_value"/>
            <result property="skuIds" column="skuIds"/>
        </collection>
    </resultMap>

    <select id="getSaleAttrsBySpuId" resultMap="skuItemSaleAttrVo">
        select ssav.attr_id,
               ssav.attr_name,
               ssav.attr_value,
               group_concat(distinct info.sku_id) skuIds
        from pms_sku_info info
                 left join pms_sku_sale_attr_value ssav on info.sku_id = ssav.sku_id
        where info.spu_id = #{spuId}
        group by ssav.attr_id, ssav.attr_name, ssav.attr_value
    </select>


</mapper>